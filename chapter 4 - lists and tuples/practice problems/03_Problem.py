#  i question it is asking to check if tupple type cannot be changed

a=(34,2345,"Jishnu")

a(2)="27"

# this will show error as tupples are immutable and its value index values cannot be changed